[project]
name = "amp-host"
version = "0.1.0"
description = "A local Flask launcher for long-lived Herdr-managed Amp agents"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
]
